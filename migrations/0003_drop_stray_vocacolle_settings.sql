-- =====================================================================
-- 0001 で削除した vocacolle_settings の作り直しを止める後始末。
--
-- db.js の初期スキーマ（起動のたびに実行される CREATE TABLE IF NOT EXISTS 群）に
-- vocacolle_settings が残っていたため、0001 が削除した直後の再起動で
-- 空のテーブルが復活していた。誰も読まないので実害は無かったが、
-- 「消したはずのテーブルがある」状態は後から見て必ず誤解のもとになる。
--
-- 初期スキーマ側からは除去済み。ここでは既に復活してしまったDBの分を消す。
-- =====================================================================

DROP TABLE IF EXISTS vocacolle_settings;
