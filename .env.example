# =====================================================================
# 常設（どの環境で動かす場合も必要）
# =====================================================================

# Discord Bot Settings
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_GUILD_ID=your_discord_server_id_here # オプション: スラッシュコマンドを特定のサーバーに即時反映させる場合
DISCORD_CHANNEL_ID=your_discord_channel_id_here # 通知を送信するチャンネルID

# データはこのマシン上の data/xenogram.sqlite に保存される（外部DB不要・設定項目なし）

TZ=Asia/Tokyo # サーバー全体のタイムゾーン指定

# 【廃止】監視対象のニコニコユーザーIDは、ここではなくDiscord上で
# サーバーごとに登録します（/user_add user_id:143305795）。
# 設定しても実行時には参照されません。
# 理由: ここに書くと、Botを新しいサーバーに入れた瞬間、そのサーバーが何も
#       登録していないのに監視が始まり、その投稿者の全動画が「新着」として
#       通知されてしまうため。新しいサーバーは完全に空の状態から始まります。
# NICO_USER_IDS=

# マイルストーン（キリ番）の判定単位。動画の再生数規模が大きくなってきたら 1000 等に
MILESTONE_STEP=100

# =====================================================================
# ボカコレ / ランキング監視
# =====================================================================
VOCACOLLE_ENABLED=true                                          # false で機能ごと停止
# 監視対象ランキングのURL（複数可・カンマ区切り。ボカコレ以外のニコニコ内ランキングも指定可）
VOCACOLLE_RANKING_URLS=https://vocaloid-collection.jp/ranking/rookie/
VOCACOLLE_CRON=5 * * * *                                        # 既定: 毎時5分 (JST)
VOCACOLLE_CHANNEL_ID=                                           # 未指定なら DISCORD_CHANNEL_ID を使用
VOCACOLLE_FETCH_TIMEOUT_MS=15000
VOCACOLLE_RANK_CHANGE_THRESHOLD=10                              # 順位がこの数値以上動いたら通知

# =====================================================================
# 急上昇検知・週次レポート
# =====================================================================
SPIKE_DETECTION_ENABLED=true
SPIKE_VIEW_THRESHOLD_PER_HOUR=500                               # 1時間あたりこの再生数以上伸びたら通知
SPIKE_COOLDOWN_HOURS=6                                          # 同じ動画への連続通知を防ぐ間隔

WEEKLY_REPORT_ENABLED=true
WEEKLY_REPORT_CRON=0 21 * * 0                                   # 既定: 毎週日曜21:00 (JST)

# =====================================================================
# X（旧Twitter）キーワード監視（読み取り専用・投稿は一切しない）
# =====================================================================
# 既定は無効。以下のセットアップを終えてから true にすること:
#   1. `uv tool install twitter-cli`（または `pipx install twitter-cli`）
#      https://github.com/public-clis/twitter-cli
#   2. 監視専用に別アカウントを用意し、ブラウザでログインしてCookieを
#      自動抽出させるか、TWITTER_AUTH_TOKEN / TWITTER_CT0 を手動設定する
#      （非公式のCookie認証のため、X社の規約上グレー。本アカウントの
#       流用は避け、凍結されても支障のないアカウントを使うこと）
TWITTER_MONITOR_ENABLED=false
TWITTER_CLI_BIN=twitter                                         # PATHが通っていなければフルパスを指定推奨
TWITTER_MONITOR_CRON=10 * * * *                                 # 既定: 毎時10分 (JST)
TWITTER_MONITOR_CHANNEL_ID=                                     # 未指定なら DISCORD_CHANNEL_ID を使用
TWITTER_MONITOR_MAX_RESULTS=20                                  # 1キーワードあたりの検索取得件数
TWITTER_MONITOR_TIMEOUT_MS=20000
# ブラウザCookieを使わない場合（サーバー環境など）は、ブラウザの開発者ツールで
# x.com の Cookie から auth_token / ct0 の値を取得して設定する
# TWITTER_AUTH_TOKEN=
# TWITTER_CT0=
# ウイルス対策ソフト等がHTTPS通信を検査（SSL/TLSスキャン）している環境では、
# その独自ルート証明書がPython側の既定のCA証明書ストアに無く通信が全て失敗することがある。
# その場合は該当ソフトのルート証明書を含むPEMバンドルを作成し、以下を指定する
# CURL_CA_BUNDLE=
# SSL_CERT_FILE=
# REQUESTS_CA_BUNDLE=

# =====================================================================
# スクリーンショット (Puppeteer)
# =====================================================================
SCREENSHOT_ENABLED=true
PUPPETEER_EXECUTABLE_PATH=                                      # システムのChromeを使う場合のみ指定。未使用なら行ごと削除してよい
SCREENSHOT_TIMEOUT_MS=60000
SCREENSHOT_VIEWPORT_WIDTH=1440
SCREENSHOT_VIEWPORT_HEIGHT=1000
SCREENSHOT_SCALE=2                                              # 2=高精細。メモリ制約のあるホスティングでは 1

# =====================================================================
# Renderにデプロイする場合のみ（PM2でのローカル/自宅サーバー運用では丸ごと不要）
# =====================================================================
PORT=47810
# 重要: 設定する場合は必ず具体的な値にする（空文字にしない）。
# 空文字にすると Puppeteer のキャッシュパス解決が壊れ、
# "Could not find Chrome" エラーで起動時にクラッシュする。
# 使わない場合は変数ごと削除すること。
PUPPETEER_CACHE_DIR=/opt/render/project/.cache/puppeteer
